name: Go

on:
  push:
    branches: [main]
    tags: ["v*"]
    paths-ignore:
      - "docs/**"
      - "**.md"
  pull_request:
    paths-ignore:
      - "docs/**"
      - "**.md"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
          cache: true
      - run: go test ./...
      - run: go test -race ./...
      - run: go vet ./...
      - run: go test -run '^$' -tags='live authenticated browser_install' ./...

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: darwin
            goarch: arm64
            output: music2bb-darwin-arm64
            browser_url: https://storage.googleapis.com/chromium-browser-snapshots/Mac_Arm/1321438/chrome-mac.zip
            browser_sha256: 4a478b542f1f6eaa160e64aab76100501e061d3bba46f6a480897ef1103c70c5
          - goos: darwin
            goarch: amd64
            output: music2bb-darwin-amd64
            browser_url: https://storage.googleapis.com/chromium-browser-snapshots/Mac/1321438/chrome-mac.zip
            browser_sha256: b25cd79dc73ebf423b05a4a0d65f772a78e3b87d394e9a2e7d0a4cc9201bda7b
          - goos: windows
            goarch: amd64
            output: music2bb-windows-amd64.exe
            browser_url: https://storage.googleapis.com/chromium-browser-snapshots/Win_x64/1321438/chrome-win.zip
            browser_sha256: f8ddee43d6865a974fc8acfcfcb9f6a42e84aadc427482ae2fa2e6a36a413454
          - goos: windows
            goarch: arm64
            output: music2bb-windows-arm64.exe
            browser_url: https://storage.googleapis.com/chromium-browser-snapshots/Win_Arm64/1321438/chrome-win.zip
            browser_sha256: 3f429a89d4da66117f8edda67b1a78cfc9fe7323415d969d74badaf162d4f61e
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
          cache: true
      - uses: actions/cache@v4
        id: bundled-browser-cache
        with:
          path: internal/browser/chromium.zip
          key: bundled-chromium-1321438-${{ matrix.goos }}-${{ matrix.goarch }}
      - name: Download bundled Chromium
        if: steps.bundled-browser-cache.outputs.cache-hit != 'true'
        env:
          BROWSER_URL: ${{ matrix.browser_url }}
        run: curl --fail --location --retry 3 --output internal/browser/chromium.zip "$BROWSER_URL"
      - name: Verify bundled Chromium
        env:
          BROWSER_SHA256: ${{ matrix.browser_sha256 }}
        run: echo "$BROWSER_SHA256  internal/browser/chromium.zip" | sha256sum --check
      - name: Build ${{ matrix.goos }}/${{ matrix.goarch }}
        env:
          CGO_ENABLED: "0"
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          OUTPUT: ${{ matrix.output }}
          PACKAGE: music2bb-${{ matrix.goos }}-${{ matrix.goarch }}
          VERSION: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || format('dev-{0}', github.sha) }}
        run: |
          mkdir -p "dist/${PACKAGE}"
          BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          go build -trimpath -tags=bundled_chromium -ldflags "-s -w -X main.version=${VERSION} -X main.commit=${GITHUB_SHA} -X main.date=${BUILD_DATE}" -o "dist/${PACKAGE}/${OUTPUT}" ./cmd/music2bb
          cp LICENSE.md THIRD_PARTY_NOTICES.md "dist/${PACKAGE}/"
          printf '%s\n' \
            "music2bb ${VERSION}" \
            "Commit: ${GITHUB_SHA}" \
            "Source repository: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}" \
            "Corresponding source: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/tree/${GITHUB_SHA}" \
            > "dist/${PACKAGE}/SOURCE.txt"
          if [[ "${GOOS}" == "windows" ]]; then
            ARCHIVE="${PACKAGE}.zip"
            cd dist
            zip -r "${ARCHIVE}" "${PACKAGE}"
            rm -rf "${PACKAGE}"
            sha256sum "${ARCHIVE}" > "${ARCHIVE}.sha256"
          else
            ARCHIVE="${PACKAGE}.tar.gz"
            tar -C dist -czf "dist/${ARCHIVE}" "${PACKAGE}"
            rm -rf "dist/${PACKAGE}"
            cd dist
            sha256sum "${ARCHIVE}" > "${ARCHIVE}.sha256"
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: music2bb-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/*
          if-no-files-found: error

  browser-smoke:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            archive: chromium-darwin-arm64.zip
            url: https://storage.googleapis.com/chromium-browser-snapshots/Mac_Arm/1321438/chrome-mac.zip
          - os: windows-latest
            archive: chromium-windows-amd64.zip
            url: https://storage.googleapis.com/chromium-browser-snapshots/Win_x64/1321438/chrome-win.zip
          - os: windows-11-arm
            archive: chromium-windows-arm64.zip
            url: https://storage.googleapis.com/chromium-browser-snapshots/Win_Arm64/1321438/chrome-win.zip
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
          cache: true
      - uses: actions/cache@v4
        id: browser-cache
        with:
          path: .browser-cache/${{ matrix.archive }}
          key: chromium-1321438-${{ matrix.os }}
      - name: Download pinned Chromium
        if: steps.browser-cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          mkdir -p .browser-cache
          curl --fail --location --retry 3 --output '.browser-cache/${{ matrix.archive }}' '${{ matrix.url }}'
      - name: Install, launch, and extract a controlled page
        shell: bash
        env:
          MUSIC2BB_TEST_BROWSER_ARCHIVE: ${{ github.workspace }}/.browser-cache/${{ matrix.archive }}
        run: go test -count=1 -tags=browser_install ./internal/browser -run TestPinnedArchiveInstallLaunchAndExtraction -v

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [test, build, browser-smoke]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: Publish release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" dist/* --verify-tag --generate-notes
